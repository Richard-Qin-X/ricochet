# file(GLOB_RECURSE ALL_TEST_SOURCES CONFIGURE_DEPENDS "*.cc")

# add_executable(ricochet_tests ${ALL_TEST_SOURCES})
# target_link_libraries(ricochet_tests PRIVATE ricochet_core)

# include(CTest)
# add_test(NAME ricochet_tests COMMAND ricochet_tests)